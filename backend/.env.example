# ConvertEasy Backend 环境变量配置示例

# 服务器配置
PORT=8080
HOST=0.0.0.0
DEBUG=false

# 公网访问地址（重要！部署到云托管时必须设置）
# 本地开发环境
# PUBLIC_BASE_URL=http://localhost:8080
# 微信云托管环境（使用你配置的域名）
PUBLIC_BASE_URL=https://convertease.site

# 文件存储目录
PUBLIC_DIR=public
UPLOAD_DIR=uploads

# Redis 配置（云托管环境推荐配置，用于多实例任务共享）
# 格式：redis://:password@host:port/db
REDIS_URL=

# 外部工具路径（通常使用默认值）
FFMPEG_PATH=ffmpeg
SOFFICE_PATH=soffice
PYTHON_PATH=python

# 转换配置
MAX_CONCURRENT=2
CONVERSION_TIMEOUT=300
CLEANUP_INTERVAL=3600
FILE_EXPIRE_TIME=86400

# PDF转换配置
PDF_LARGE_FILE_THRESHOLD_MB=20
PDF_STREAM_PROCESSING=true

# 速率限制
RATE_LIMIT_POINTS=120
RATE_LIMIT_DURATION=60
